apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Uses the container name — Grafana talks to Prometheus internally
    url: http://prometheus:9090
    isDefault: true
    editable: true