apiVersion: 1

providers:
  - name: "CloudWatch Dashboards"
    orgId: 1
    type: file
    disableDeletion: false
    editable: true
    options:
      # Grafana looks in this folder for dashboard JSON files
      path: /etc/grafana/provisioning/dashboards-json